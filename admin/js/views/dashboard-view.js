(() => { "use strict"; window.AdminViews = window.AdminViews || {}; window.AdminViews.dashboard = () => { const root=document.createElement("section"); root.innerHTML='<header class="admin-view-header"><p class="admin-eyebrow">Resumen</p><h2>Dashboard</h2><p class="admin-view-copy">La información real se incorporará en la siguiente etapa.</p></header><div class="admin-placeholder-grid"></div>'; const grid=root.querySelector("div"); ["Invitaciones","Confirmaciones","Asistencia"].forEach((name)=>{const card=document.createElement("article");card.className="admin-placeholder-card";const title=document.createElement("h3");title.textContent=name;const status=document.createElement("span");status.textContent="Próximamente";card.append(title,status);grid.appendChild(card);}); return root; }; })();
